extends layout

block content
    if group
        p= group.name
        each member in group.member
            p= member.id
            p= member.first_name

    if event
        p= event.title
        p= event.description
        for member in event.member
            p= member.username